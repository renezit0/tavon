{
  "name": "@tavon/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Tavon Cardápio — Android APK via Capacitor",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
